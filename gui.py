from scheduler import Scheduler


class Gui:
    def __init__(self, everything, scheduler):
        self.everything = everything
        self.scheduler = scheduler

    def factory_method(self, ):

    def make_schedule(self, scheduler):
        pass
